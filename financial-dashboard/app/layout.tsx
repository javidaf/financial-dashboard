import '@/app/ui/global.css';
import { inter } from '@/app/ui/fonts';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: {
    template: '%s | Financial Dashboard',
    default: 'Financial Dashboard',
  },
  description:
    'A dashboard for managing invoices and customers. Built with Next.js 14 and its App Router.',
  metadataBase: new URL('https://financial-dashboard.vercel.sh'),
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={`${inter.className} antialiased`}>{children}</body>
    </html>
  );
}
